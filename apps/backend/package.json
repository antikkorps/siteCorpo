{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend PocketBase pour le monorepo",
  "private": true,
  "scripts": {
    "dev": "./pocketbase serve --http=127.0.0.1:8090",
    "start": "./pocketbase serve --http=127.0.0.1:8090",
    "build": "echo 'PocketBase est un exécutable, pas besoin de build'"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 